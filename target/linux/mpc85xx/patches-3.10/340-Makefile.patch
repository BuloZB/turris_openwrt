--- a/drivers/video/Makefile
+++ b/drivers/video/Makefile
@@ -177,3 +177,5 @@ obj-$(CONFIG_VIDEOMODE_HELPERS) += displ
 ifeq ($(CONFIG_OF),y)
 obj-$(CONFIG_VIDEOMODE_HELPERS) += of_display_timing.o of_videomode.o
 endif
+
+obj-y += fbtft/
